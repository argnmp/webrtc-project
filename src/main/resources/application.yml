server:
  port: 8080
  address: 0.0.0.0

spring:
  application:
    name: hycord
  mvc:
    static-path-pattern: /static/**

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

---
spring:
  config:
    activate:
      on-profile: local, prod